# Ignorar backups de banco de dados
backup-*.db

# Qualquer banco SQLite ou sidecar WAL solto na raiz do repo (KI-029 — o padrão anterior não cobria
# nomes como "database-pre-cleanup-*.db", só "database.db*"/"backup-*.db")
/*.db
/*.db-shm
/*.db-wal
# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/

# Build / dist desktop
*.spec
build/
dist/
build_irflow/
build_irflow2/
build_irflow_setup/
installer_output/
release/

# Dados locais (não versionar banco nem backups)
database.db*
backups/
data/price_tables.json

# Temporários
tmp*/
temp_output.txt
*.bak
*.old
*.log

# Cobertura de testes (gerado por pytest-cov)
.coverage
htmlcov/

# Gerado em runtime — não precisa ir pro repo
integrations.json

# Artefatos do graphify (knowledge graph local, não versionado)
graphify-out/

# Permissões locais do Claude Code (por máquina, não compartilhar)
.claude/settings.local.json

.openclaude-profile.json.openclaude-profile.json
.openclaude-profile.json
.openclaude-profile.json

.env
.env.local
.env.development
.env.production

# Rascunhos de exploração de identidade visual (HTML soltos, canvases) — não é entregável
# versionado; deliberados (ícone/wordmark/paleta) vão para docs/company/BRAND_IDENTITY.md e
# assets finais para frontend/public/brand/ quando prontos para uso no produto.
/Brand Indetity/
